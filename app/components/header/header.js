var React = require('react')

module.exports = (
    <header className="header section section__padded">
        <a href="/">
            <h1 className="header__title">{process.env.SITE_NAME}</h1>
        </a>
    </header>
)